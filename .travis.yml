language: python
before_script:
  - sudo add-apt-repository -y ppa:freecad-maintainers/freecad-stable
  - sudo apt update
  - sudo apt-get install -y freecad
  - sudo apt-get install -y qt5-default
script:
  - freecad -t AeroFoil.FCMacro
